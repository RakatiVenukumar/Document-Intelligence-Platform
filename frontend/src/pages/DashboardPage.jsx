export default function DashboardPage() {
  return (
    <section className="rounded-[2rem] border border-black/10 bg-white/80 p-8 shadow-glow backdrop-blur-xl">
      <p className="font-display text-xs font-semibold uppercase tracking-[0.35em] text-accent">Dashboard</p>
      <h2 className="mt-3 font-display text-3xl font-semibold sm:text-4xl">Book catalog and intelligence workspace</h2>
      <p className="mt-4 max-w-2xl text-sm leading-7 text-slate-600 sm:text-base">
        The data-fetching and book listing flow will land here in the next step. This shell is already wired for a
        polished, responsive dashboard layout.
      </p>
    </section>
  )
}
